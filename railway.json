{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "composer install --no-dev --optimize-autoloader --no-interaction --no-scripts && yarn install --frozen-lockfile && yarn build"
  },
  "deploy": {
    "preDeployCommand": "php artisan migrate --force",
    "startCommand": "php artisan storage:link || true && php -S 0.0.0.0:$PORT -t public",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
